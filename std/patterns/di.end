// ?? End Language: std/patterns/di.end
// Compile-Time Zero-Overhead Dependency Injection Container

pub st DiContainer {
    service_count: i32,
    is_locked: bool,
}

pub fn di_container_create() DiContainer {
    ret DiContainer {
        service_count: 0,
        is_locked: false,
    }
}
