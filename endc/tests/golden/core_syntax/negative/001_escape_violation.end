// @test: negative
// @expect-error: E0903

st Buffer {
    size: i64,
}

fn escape_ptr() *Buffer {
    region a {
        val b: *Buffer = alloc [1]Buffer
        ret b
    }
}

fn main() void {
    escape_ptr();
}
