// @test: positive
// @expect-stdout:
// Submit
// true

st ButtonWidget {
    label: str,
    is_active: bool,
}

fn main() void {
    val btn = ButtonWidget { label: "Submit", is_active: true }
    println(btn.label)
    println(btn.is_active)
}
