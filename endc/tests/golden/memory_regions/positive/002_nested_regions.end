// @test: positive
// @expect-stdout:
// Outer enter
// Inner arena
// Outer exit

fn main() void {
    region outer {
        println("Outer enter")
        region inner {
            println("Inner arena")
        }
        println("Outer exit")
    }
}
