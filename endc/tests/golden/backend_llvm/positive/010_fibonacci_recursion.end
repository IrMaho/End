// @test: positive
// @expect-stdout:
// 55

fn fib(n: i64) -> i64 {
    if n <= 0 {
        return 0
    }
    if n == 1 {
        return 1
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> i32 {
    val ans = fib(10)
    println(ans)
    return 0
}
